(function(){
  var date, mood, options;
  if (singing) {
    mood = greatlyImproved;
  }
  if (happy && knowsIt) {
    clapsHands();
    chaChaCha();
  }
  date = friday ? sue : jill;
  options = options || defaultOptions;
})();
